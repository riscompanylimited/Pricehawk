version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: pricehawk_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: pricehawk
      POSTGRES_PASSWORD: pricehawk_secret
      POSTGRES_DB: pricehawk
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./cfw:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pricehawk"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
